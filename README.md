# Scala Auxiliary Constructor Exception Handling

This repository demonstrates a subtle bug related to exception handling in Scala's auxiliary constructors. The `MyClass` example shows how a seemingly straightforward conversion within an auxiliary constructor can lead to unexpected behavior if the conversion fails. The solution explores safer approaches.